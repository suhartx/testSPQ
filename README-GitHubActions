1. Update maven.yml to:

name: Java CI with Maven

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Set up JDK 11
      uses: actions/setup-java@v2
      with:
        java-version: '11'
        distribution: 'adopt'
    - name: Build with Maven
      run: ls -la && pwd && mvn test -DargLine="-Dcontiperf.active=false"

3. Go to GitHub actions dashboard:

https://github.com/<repo_id>/actions

4. Browse through the builds

5. Follow instructions of GitHub actions documentation: https://docs.github.com/en/actions

renaming branches: https://dev.to/afrodevgirl/replacing-master-with-main-in-github-2fjf
changing default branch: https://www.google.com/search?q=how+to+change+default+branch+in+github&oq=how+to+change+default+branch+&aqs=chrome.0.0i20i263j69i57j0l8.5639j0j7&sourceid=chrome&ie=UTF-8


